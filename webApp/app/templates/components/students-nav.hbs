<ul class="nav-sideBar">
	{{#each Nav.program as |program|}}
		<ul>
			<p>{{program.name}}</p>
			{{#each program.semester as |semester|}}
				<ul>
					<p>{{semester.name}}</p>
					{{#each semester.student as |student|}}
						<ul>
							<li {{action navSelection}}>
								{{student.name}}
							</li>			
						</ul>
					{{/each}}					
				</ul>
			{{/each}}
		</ul>
	{{/each}}
</ul>
{{yield}}
