{{#each items.actionList as |role|}}
    <div class={{if (is-active-route currentPath role.actionName) "active-nav-secondary li-horizontal" "li-horizontal"}}>
		{{#link-to (to-lowercase (concat  items.name "." role.actionName))}}
			{{role.actionName}}
		{{/link-to}}
	</div>
{{/each}}


{{yield}}
